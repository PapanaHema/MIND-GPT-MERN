{
  "name": "mindgpt",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run check",
    "start": "concurrently -k -s first \"npm:server\" \"npm:client\"",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "server": "node server",
    "client": "wait-on http-get://localhost:8999/api/health && npm --prefix frontend start"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "wait-on": "^9.0.10"
  }
}
