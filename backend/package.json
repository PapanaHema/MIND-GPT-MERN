{
  "name": "mindgpt-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check config/database.js && node --check controllers/authController.js && node --check controllers/chatController.js && node --check models/User.js",
    "migrate:users": "node scripts/migrate-json-users.js"
  },
  "dependencies": {
    "@google/genai": "^2.8.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.1",
    "validator": "^13.15.35"
  }
}
